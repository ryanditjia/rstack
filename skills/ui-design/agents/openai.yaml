interface:
  display_name: "UI Design"
  short_description: "Focused UI adjustments and explicit design modes"
  default_prompt: "Use $ui-design tune to refine the named existing interface."
policy:
  allow_implicit_invocation: true
