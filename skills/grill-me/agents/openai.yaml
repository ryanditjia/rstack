interface:
  display_name: "Grill Me"
  short_description: "Expose hidden branches in a decision"
  default_prompt: "Use $grill-me to stress-test this plan through a structured interview."

policy:
  allow_implicit_invocation: false
